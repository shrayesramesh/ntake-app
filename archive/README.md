# Archive — superseded documents

These were superseded by the consolidated **`spec/`** doc set (the current
source of truth) on 2026-08-30. Kept for history only; do not use for building.

- `REQUIREMENTS.md`, `DESIGN.md`, `PLAN.md` — pre-reframe versions. Superseded by
  `spec/REQUIREMENTS.md`, `spec/DESIGN.md`, `spec/PLAN.md`.
- `REQUIREMENTS_UPDATE.md` — the append-log of changes that were folded into the
  clean `spec/REQUIREMENTS.md`.
- `AGENT_START_HERE.md`, `agent_bootstrap_prompt.md`, `agent_handoff_manifest.md`
  — old agent entry points pointing at the pre-reframe docs/checkpoints.
  Superseded by `spec/AGENT_START_HERE.md`.

The reasoning trail (why decisions were made) lives in `research/` at repo root,
which is intentionally **not** archived.
