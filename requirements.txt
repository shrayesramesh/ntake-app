# Family calendar — pinned dependencies (verified working, 2026-08, Python 3.12)
# Core
fastapi==0.141.1
uvicorn==0.52.4
pydantic==2.13.5
starlette==1.6.0

# Data
SQLAlchemy==2.0.52
alembic==1.19.1   # DB migrations (wrapped in app.manage; see app/migrations.py)

# SSE (checkpoint 1e)
sse-starlette==3.4.8

# Tests
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0   # coverage measurement (make coverage; --cov gate in make check)
httpx==0.28.1       # required: FastAPI TestClient imports it
# Note: pytest shows one cosmetic StarletteDeprecationWarning about httpx/httpx2.
# It is harmless; httpx is required and httpx2 is intentionally NOT installed.

# Lint / format / types
ruff==0.16.5
mypy==2.3.1
