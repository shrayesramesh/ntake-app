AVAILABLE TOOLS:

WORK ITEMS — create and state
- create_work_item: Create a new work item (a task/todo). — params: title: string, description: string?, tags: array<string>?, checklist_items: array<string>?
- start_work_item: Start work on an item (move it to Doing). — params: (no params)
- move_to_on_deck: Move a work item to On deck (queued up next). — params: (no params)
- move_to_todo: Move a work item back to Todo. — params: (no params)
- complete_work_item: Mark a work item done. — params: (no params)
- reopen_work_item: Reopen a completed item (back to Todo; clears completion). — params: (no params)
- archive_work_item: Archive a work item (only a done item may be archived). — params: (no params)
- archive_all_done: Archive every unarchived Done work item in the family. — params: (no params)

WORK ITEMS — details
- append_update: Append assistant context to an existing work item. — params: body: string
- assign_work_item: Assign a work item to a family member. — params: member_id: integer
- set_due_date: Set a work item's due date. — params: due_at: datetime
- set_work_item_tags: Replace a work item's complete shared tag list. — params: tags: array<string>

CHECKLISTS
- add_checklist_items: Add checklist items (e.g. a grocery list) to a work item. — params: items: array<string>
- check_off_items: Mark named checklist items complete. — params: items: array<string>

EVENTS — create and timing
- create_timed_event: Create a timed calendar event. — params: title: string, start_at: datetime, end_at: datetime, description: string?, location: string?, participants: array<string>?, tags: array<string>?
- create_all_day_event: Create an all-day calendar event. — params: title: string, start_date: date, end_date: date?, description: string?, location: string?, participants: array<string>?, tags: array<string>?
- reschedule_timed_event: Move an existing event to a timed range. — params: start_at: datetime, end_at: datetime
- reschedule_all_day_event: Move an existing event to all-day date(s). — params: start_date: date, end_date: date?

EVENTS — details
- set_event_location: Set an existing event's location. — params: location: string
- add_event_participants: Add people by name to an existing event. — params: participants: array<string>
- set_event_tags: Replace an existing event's complete shared tag list. — params: tags: array<string>
- delete_event: Delete an existing event (e.g. it was cancelled). — params: (no params)
- deconflict_events: Move an event to the next day to resolve a same-time conflict. — params: (no params)

NO ACTION
- no_action: Nothing to suggest. — params: (no params)

CAPTURE:
FROM: [m1] Alex (adult)
NOTE: "he's coming friday at 3"

CONTEXT:
RELEVANT WORK ITEMS:
- [w2] call plumber (doing)

RELEVANT EVENTS:
- (none)
